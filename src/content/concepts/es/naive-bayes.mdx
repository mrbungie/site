---
title: "Naive Bayes"
summary: "Un clasificador probabilístico rápido que asume independencia condicional entre las variables."
tags: ["machine-learning", "probabilidad", "clasificación"]
area: "statistics"
level: "intermediate"
prerequisites: ["regresion-logistica"]
related: ["regresion-logistica", "svm", "knn"]
featured: false
draft: false
---

import NaiveBayesVisualizer from '../../../components/visuals/NaiveBayesVisualizer';
import Math from '../../../components/Math';
import TaxonomyBox from '../../../components/concept/TaxonomyBox.astro';

**Naive Bayes** usa el teorema de Bayes para estimar qué clase es más probable dada la evidencia observada. Su supuesto “naive” es que las variables son condicionalmente independientes dentro de cada clase.

<TaxonomyBox
  modelType="Un modelo probabilístico generativo que estima distribuciones por clase y combina evidencia bajo independencia condicional."
  optimizationMethods={[
    "Estimación por máxima verosimilitud de probabilidades o parámetros de distribución",
    "Estimación MAP con suavizado de Laplace o priors conjugados",
    "Cálculo cerrado de conteos, medias y varianzas por clase según la variante elegida",
  ]}
  typicalOptimization="Estimación cerrada por máxima verosimilitud con suavizado"
  typicalLoss="Negative log-likelihood o log loss generativa"
/>

## Combina evidencia paso a paso

<div class="my-10 not-prose">
  <NaiveBayesVisualizer client:load />
</div>

## La regla de decisión

<Math formula={"P(C|X) = \\frac{P(X|C)P(C)}{P(X)}"} block />

En la práctica se comparan las probabilidades posteriores de cada clase y se elige la mayor.

## Qué observar

- Con pocas cuentas ya se obtiene una probabilidad posterior útil.
- El supuesto de independencia es fuerte, pero muchas veces funciona sorprendentemente bien.
- Es especialmente competitivo en texto y problemas de alta dimensionalidad.

## Aplicaciones

Filtrado de spam, análisis de sentimiento, clasificación documental y tareas donde importa mucho la velocidad de entrenamiento y predicción.
