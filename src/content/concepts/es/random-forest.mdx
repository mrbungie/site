---
title: "Random Forest"
summary: "Un ensamble de árboles que votan juntos para ganar robustez y reducir la varianza."
tags: ["machine-learning", "ensamble", "clasificación"]
area: "statistics"
level: "intermediate"
prerequisites: ["arbol-de-decision"]
related: ["arbol-de-decision", "svm", "knn"]
featured: false
draft: false
---

import RandomForestVisualizer from '../../../components/visuals/RandomForestVisualizer';
import TaxonomyBox from '../../../components/concept/TaxonomyBox.astro';

**Random Forest** combina muchos árboles de decisión entrenados sobre muestras y subconjuntos de variables distintos. En lugar de confiar en una sola rama, agrega muchas voces pequeñas y decide por mayoría.

<TaxonomyBox
  modelType="Un ensamble bagging de múltiples árboles de decisión con muestreo bootstrap y selección aleatoria de variables por nodo."
  optimizationMethods={[
    "Crecimiento greedy de árboles CART dentro de cada árbol individual",
    "Bootstrap aggregation (bagging) y feature subsampling para decorrelacionar el ensamble",
    "Selección por validación de hiperparámetros como número de árboles, profundidad y mtry",
  ]}
  typicalOptimization="Bagging + árboles CART greedy con feature subsampling"
  typicalLoss="Impureza Gini o entropía en los nodos de cada árbol; voto mayoritario como regla final del ensamble"
/>

## Mira cómo vota el bosque

<div class="my-10 not-prose">
  <RandomForestVisualizer client:load />
</div>

## Intuición clave

Un árbol individual puede ser inestable: un pequeño cambio en los datos puede alterar mucho su estructura. El bosque reduce ese problema porque promedia decisiones diversas.

## Qué observar

- Cada árbol usa reglas parecidas, pero no idénticas.
- La decisión final depende de la **agregación**, no de un solo árbol.
- Se gana robustez a costa de perder una explicación global simple.

## Aplicaciones

Es un baseline fuerte para clasificación tabular, detección de fraude, riesgo crediticio y diagnóstico cuando se busca buen desempeño sin demasiado ajuste fino.
